#!/usr/bin/env bash
set -e

# Lint markdown
yarn add global textlint \
                prettier \
                remark-cli \
                remark-lint 
echo "Also run yarn install from the root of ~/.oroshi"
