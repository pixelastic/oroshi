#!/usr/bin/env zsh
# Returns exit 0 if <commit-a> is a strict ancestor of <commit-b>
# Usage:
# $ git-commit-is-before <commit-a> <commit-b>

# Same ref is not strictly before itself
[[ "$1" == "$2" ]] && exit 1

git merge-base --is-ancestor "$1" "$2"
