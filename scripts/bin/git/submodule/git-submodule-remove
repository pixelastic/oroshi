#!/usr/bin/env zsh
# Remove a submodule from a repo
# Usage:
# $ git-submodule-remove ./path/to/submodule # Removes the submodule
# $ git-submodule-remove ./one ./two         # Removes multiple submodules

# TODO: Fails if not a submodule
# TODO: Fails if not valid directories

#      `git rm #{@full_path}`
#      `git config -f .git/config --remove-section submodule.#{@relative_path}`
#      `rm -rf .git/modules/#{@relative_path}`
