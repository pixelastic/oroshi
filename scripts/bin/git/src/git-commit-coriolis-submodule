#!/usr/bin/env zsh
# Update the ./scripts/bin/coriolis submodule with message:
# chore(vit): Update coriolis submodule

cd "$(git-directory-root -f)"
git add ./scripts/bin/coriolis
git commit -m "chore(coriolis): Update coriolis submodule"
