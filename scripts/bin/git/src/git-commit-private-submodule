#!/usr/bin/env zsh
# Update the ./private submodule at the root of the repo with the message:
# chore(private): Update private submodule

cd "$(git-directory-root -f)"
git add ./private
git commit -m "chore(private): Update private submodule"
