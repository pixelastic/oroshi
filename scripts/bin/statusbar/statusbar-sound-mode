#!/usr/bin/env zsh
# JSON info about if sound mode is enabled
source $ZSH_CONFIG_PATH/theming/env/colors.zsh

local storeFile=$OROSHI_TMP_FOLDER/sound-mode/on

if [[ ! -f ${storeFile} ]]; then
  echo "[]"
  exit 0
fi

jo text="  " fg=$COLOR_ALIAS_AI_ENABLED | jo -a
