#!/usr/bin/env zsh
# Return an array of all words in the input
# Usage:
# $ text-words-to-line "one two three"
# $ echo "one two three" | text-words-to-line

# Read from arg or from pipe
local input=$1
[[ -p /dev/stdin ]] && input="$(cat -)"

echo $input | text-trim | tr " " "\n"
