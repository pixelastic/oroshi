#!/usr/bin/env zsh
# Fuzzy finding in all subfiles.
local selection="$(\
f \
  --color always \
  --type file \
    | fzf \
      --ansi \
      --preview 'fzf-preview {}' \
)"
selection=${selection:gs/ /\\ /}

echo $selection
