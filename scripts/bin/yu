#!/usr/bin/env bash
# This makes an update of a given package, taking the latest available
set -e

rawDeps=("$@")
deps=()
for depName in "${rawDeps[@]}"; do
  deps+=("${depName}@latest")
done

yarn upgrade "${deps[@]}"
