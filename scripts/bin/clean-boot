#!/usr/bin/env bash

dpkg --get-selections \
 | grep 'linux-image*' \
 | awk '{print $1}' \
 | egrep -v "linux-image-$(uname -r) | linux-image-generic" \
 | while read n; do sudo apt-get -y remove $n; done

sudo apt-get upgrade
sudo apt-get update
