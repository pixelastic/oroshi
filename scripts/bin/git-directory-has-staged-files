#!/usr/bin/env zsh
# Check if current directory has elements staged

# Not a git repo, can't have anything staged
if ! git-is-repository; then
  exit 1
fi

git status --short --porcelain | grep '^. ' &>/dev/null

