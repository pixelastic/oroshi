#!/usr/bin/env zsh
# Display all the directories of a given directory
# Usage:
# $ fzf-directories-search-source /path/to/directory

local directory="$1"

cd $directory
f \
  --base-directory "$directory" \
  . \
  --color always \
  --type directory
