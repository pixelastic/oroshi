#!/usr/bin/env zsh
# Starts a dynamic regexp search in file, in the current project

local gitRoot="$(git-directory-root -f)"

fzf-regexp-search \
  "fzf-regexp-search-project-source" \
  "${gitRoot}"
