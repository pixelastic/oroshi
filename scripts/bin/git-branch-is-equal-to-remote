#!/usr/bin/env zsh
# Check if the current branch is equal to the remote branch

local remoteHead=$(git rev-parse @{upstream} 2>/dev/null)
local localHead=$(git rev-parse @{0} 2>/dev/null)

# local is equal to remote, nothing changed
if [[ "$remoteHead" = "$localHead" ]]; then
  exit 0
fi

exit 1

