#!/usr/bin/env zsh

local sessionName="$1"
local windowName="$2"

local sessionColor="$(tmux-status-color $sessionName)"
local windowIcon="$(tmux-status-icon $windowName)"
echo -n " ${windowIcon}${windowName} #[fg=colour239]#[default]";
