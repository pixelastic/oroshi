#!/usr/bin/env zsh
# Reload Kitty tab bar modules and redraw
# Usage:
# $ kitty-reload
set -e

# Write the Reload Beacon
local beaconDir="$OROSHI_TMP_FOLDER/kitty/beacons"
mkdir -p "${beaconDir}"
touch "${beaconDir}/reload"

# Force immediate tab bar redraw
kitty-redraw
