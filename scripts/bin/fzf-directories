#!/usr/bin/env zsh
# Fuzzy finding in all subdirectories
local selection="$(\
f \
  --color always \
  --type directory \
    | fzf \
      --ansi \
      --preview 'fzf-preview {}' \
)"


echo ${selection:gs/ /\\ /}

