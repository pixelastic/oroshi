#!/usr/bin/env zsh
# Convert markdown files to HTML using pandoc
# Usage:
# $ md2html file.md              # Single file
# $ md2html file1.md file2.md    # Multiple files
set -e

for file in $@; do
  pandoc "$file" -o "${file:a:r}.html"
  echo "✔ ${file:r} converted to html"
done
