#!/usr/bin/env zsh
# Return the symbol for a specific name
# Usage:
#   tmux-status-symbol projects
source ~/.oroshi/config/zsh/theming/projects.zsh

local projectSlug="$1"

echo -n $PROJECT_ICON[$projectSlug]
