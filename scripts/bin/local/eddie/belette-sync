#!/usr/bin/env ruby
# encoding : utf-8
belette_sd = File.join('/', 'media', 'BELETTE')
belette_books = File.join(belette_sd, 'Books')
belette_rp_books = File.join(belette_sd, 'Roleplay', 'books')

eddie_books = File.join('~', 'Documents', 'books')
eddie_rp_books = File.join('~', 'Documents', 'roleplay', 'books')

def execute(cmd)
	IO.popen(cmd) do |io| 
		while (line = io.gets) do 
			puts line 
		end
	end
end

# Will update the book collection
puts "# Updating book list"
execute "ebook-sync #{eddie_books} #{belette_books}"

# Update roleplay repositories
puts "# Updating roleplay books"
execute "update-dir #{eddie_rp_books} #{belette_rp_books}"

