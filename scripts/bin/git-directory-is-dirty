#!/usr/bin/env zsh
# Check if current directory is dirty, ie. contains modified files, deleted or
# newly added files

# Not a git repo, can't be dirty
if ! git-is-repository; then
  exit 1
fi

git status --short --porcelain | grep '^\(M \|D \|\?\?\)' &>/dev/null
