#!/usr/bin/env zsh
# Display the list of all linked modules
local node_modules="$(yarn bin 2>/dev/null)/.."
echo "" && return
local modules="$(find $node_modules -maxdepth 1 -type l -printf "%f\n" 2>/dev/null)"

for module in $modules; do
  echo "=> $module"
done

