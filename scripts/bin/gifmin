#!/usr/bin/env zsh
if [[ $1 == '-h' ]]; then
  echo "Usage:"
  echo "gifmin ./input.gif [./input2.gif]"
  echo ""
  echo "Compress GIF files"
  exit 1
fi

for file in $@; do
  gifsicle -O3 --colors 256 -o ${file:a} ${file:a}
done
