## Execution order

01-dockerfile                         → start here, no blockers
02-basic-sandbox                      → needs 01
03-workspace-flag     \
04-passthrough-args    |
07-claude-config-mount |              → all need 02, can run in parallel
08-api-key-guard       |
09-worktree-support   /
05-image-build                        → needs 01 + 02
06-node-version                       → needs 05

## Guidance

Each issue follows TDD: write a failing bats test first, then implement until
it passes. Do not skip the red step.

Tests live in scripts/bin/__tests__/ (existing bats setup in the repo).

The Dockerfile and script both live in scripts/bin/claude/claude-sandbox/.
Create that directory when starting issue-01.

---
## Log (append below when an issue is completed)

