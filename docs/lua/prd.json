[
  {
    "id": "0002",
    "description": "lua-lint-selene reports a violation on a file with a known selene error",
    "steps": [
      "Create a temporary Lua file containing a known selene violation (e.g. undefined global usage)",
      "Run `lua-lint-selene <file>`",
      "Assert output contains a line matching the ▮-separated format with the correct code",
      "Assert exit code is 1"
    ],
    "passes": false
  },
  {
    "id": "0002",
    "description": "lua-lint-selene exits 0 with no output on a clean Lua file",
    "steps": [
      "Create a temporary Lua file with no selene violations",
      "Run `lua-lint-selene <file>`",
      "Assert output is empty",
      "Assert exit code is 0"
    ],
    "passes": false
  },
  {
    "id": "0003",
    "description": "rule-no-vim-deepcopy flags a line containing vim.deepcopy(",
    "steps": [
      "Create a temporary Lua file containing `local x = vim.deepcopy(t)` on a known line number",
      "Run `lua-lint-custom <file>`",
      "Assert output contains a violation line with code `noVimDeepcopy` at the correct line number",
      "Assert exit code is 1"
    ],
    "passes": false
  },
  {
    "id": "0003",
    "description": "rule-no-vim-deepcopy produces no output on a file without vim.deepcopy",
    "steps": [
      "Create a temporary Lua file with no calls to vim.deepcopy",
      "Run `lua-lint-custom <file>`",
      "Assert output is empty",
      "Assert exit code is 0"
    ],
    "passes": false
  },
  {
    "id": "0004",
    "description": "lua-lint merges violations from both selene and custom rules",
    "steps": [
      "Create a temporary Lua file containing both a selene violation and a vim.deepcopy( call",
      "Run `lua-lint <file>`",
      "Assert JSON output contains at least two violation objects",
      "Assert one object has a selene-originated code and one has noVimDeepcopy",
      "Assert exit code is 1"
    ],
    "passes": false
  },
  {
    "id": "0004",
    "description": "lua-lint outputs empty JSON array and exits 0 on a clean file",
    "steps": [
      "Create a temporary Lua file with no selene violations and no custom rule violations",
      "Run `lua-lint <file>`",
      "Assert output is `[]`",
      "Assert exit code is 0"
    ],
    "passes": false
  },
  {
    "id": "0004",
    "description": "lua-lint exits 1 when only custom violations are present",
    "steps": [
      "Create a temporary Lua file with only a vim.deepcopy( call (no selene violations)",
      "Run `lua-lint <file>`",
      "Assert JSON output contains exactly one violation with code noVimDeepcopy",
      "Assert exit code is 1"
    ],
    "passes": false
  },
  {
    "id": "0010",
    "description": "lua-test-path returns the path when given an existing _spec.lua file",
    "steps": [
      "Create a temporary `foo_spec.lua` file",
      "Run `lua-test-path foo_spec.lua`",
      "Assert output is the path to `foo_spec.lua`",
      "Assert exit code is 0"
    ],
    "passes": false
  },
  {
    "id": "0010",
    "description": "lua-test-path exits 1 when given a _spec.lua path that does not exist",
    "steps": [
      "Run `lua-test-path /nonexistent/foo_spec.lua`",
      "Assert output is empty",
      "Assert exit code is 1"
    ],
    "passes": false
  },
  {
    "id": "0010",
    "description": "lua-test-path resolves source file to its spec when spec exists",
    "steps": [
      "Create a temporary directory with `lodash.lua` and `__tests__/lodash_spec.lua`",
      "Run `lua-test-path <dir>/lodash.lua`",
      "Assert output is `<dir>/__tests__/lodash_spec.lua`",
      "Assert exit code is 0"
    ],
    "passes": false
  },
  {
    "id": "0010",
    "description": "lua-test-path exits 1 silently when no spec exists for a source file",
    "steps": [
      "Create a temporary directory with `lodash.lua` and no `__tests__/` directory",
      "Run `lua-test-path <dir>/lodash.lua`",
      "Assert output is empty",
      "Assert exit code is 1"
    ],
    "passes": false
  },
  {
    "id": "0011",
    "description": "lua-test runs a passing spec and exits 0",
    "steps": [
      "Run `lua-test config/vim/nvim/config/functions/__tests__/lodash_spec.lua`",
      "Assert exit code is 0",
      "Assert output contains the mini.test pass indicator"
    ],
    "passes": false
  },
  {
    "id": "0011",
    "description": "lua-test resolves a source file to its spec and runs it",
    "steps": [
      "Run `lua-test config/vim/nvim/config/functions/lodash.lua`",
      "Assert exit code is 0 (spec exists and passes)"
    ],
    "passes": false
  },
  {
    "id": "0011",
    "description": "lua-test exits 0 silently when called on a file with no spec",
    "steps": [
      "Run `lua-test config/vim/nvim/config/functions/hacks.lua` (no spec exists for this file)",
      "Assert exit code is 0",
      "Assert output is empty"
    ],
    "passes": false
  }
]
