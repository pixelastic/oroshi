#!/usr/bin/env zsh

echo "Install ExtensionManager"
sudo apt-get install \
	gnome-shell-extensions \
	gnome-shell-extension-manager

echo "Install the following extensions from the extensions.gnome.org website:"
echo "You might need to install the dedicated Chrome extensions as advertised on GNOME website"
echo "- EasyScreenCast"
echo "    => Record screen video and webcam"
echo "- Frippery Move Clock"
echo "    => Move clock on the right of the topbar"
echo "- Unite"
echo "    => Merge the window top bar with the screen topbar"
