# Display list of all globally installed skills with descriptions
function complete-skills() {
  local rawList="$(skill-list-raw)"

  for rawLine in ${(f)rawList}; do
    local split=(${(@s/▮/)rawLine})
    local skillName=$split[1]
    local skillDescription=$split[2]
    echo "${skillName}:${skillDescription}"
  done
}
