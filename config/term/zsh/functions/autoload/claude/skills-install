function skills-install() {
  local skillName="$1"

  if [[ -z "$skillName" ]]; then
    echo "Usage: skills-install <skill-name>"
    return 1
  fi

  skills add "$skillName" \
    --global \
    --agent claude-code \
    --yes >/dev/null
  echo "Skill ${skillName} installed"
}
