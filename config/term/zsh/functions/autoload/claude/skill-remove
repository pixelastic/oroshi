function skill-remove() {
  local skillName="$1"

  if [[ -z "$skillName" ]]; then
    echo "Usage: skill-remove <skill-name>"
    return 1
  fi

  skills remove "$skillName" \
    --global \
    --yes
}
