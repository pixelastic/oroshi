function fzf-claude-sessions-options() {
  # Prompt styled like Claude project (white on amber background)
  local promptPrefix="$(colorize " ● Claude " $COLOR_GRAY_2 $COLOR_AMBER_7)"
  local promptSuffix="$(colorize "" $COLOR_AMBER_7)"
  echo "--prompt=${promptPrefix}${promptSuffix} "

  echo "--delimiter=▮"
  echo "--with-nth=2"
  echo "--preview=fzf-claude-sessions-preview {1}"
  echo "--preview-window=wrap"

}
