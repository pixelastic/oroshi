# Display the prompt for finding a file in a given directory
function fzf-fs-files-subdir-prompt() {
	local currentDirectory="$(fzf-fs-shared-pwd-subdir)"
	echo "$(fzf-fs-files-shared-prompt $currentDirectory) "
}
