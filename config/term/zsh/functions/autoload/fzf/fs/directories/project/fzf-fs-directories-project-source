# Returns a list of directories, in the current project
function fzf-fs-directories-project-source() {
	local currentDirectory="$(fzf-fs-shared-pwd-project)"
	fzf-fs-directories-shared-source "${currentDirectory}"
}
