# Returns the owner part from the GitHub remote URL.
# Usage:
# $ git-github-project-owner              # pixelastic
# $ git-github-project-owner upstream     # someorg

local project="$(git-github-project "$@")"
[[ "$project" == "" ]] && return 1

echo "${project%%/*}"
