# Push current worktree into main
# Usage:
# $ git-worktree-push
setopt local_options errexit

local currentBranch="$(git-branch-current)"
local mainPath="$(git-worktree-main)"

git -C "$mainPath" merge --ff-only "$currentBranch"
