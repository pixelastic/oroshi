! kyra is my temporary Dell Latitude 7300 

! PageDown/PageUp, next to arrow keys are actually LineStart/LineEnd
keycode 112 = Home
keycode 117 = End
! Same in reverse
keycode 95 = Prior
keycode 96 = Next

! The keyboard does not have a PrintScreen button next to the arrow keys
! We'll use the Right Ctrl that we don't need instead
keycode 105 = Control_R NoSymbol Control_R
