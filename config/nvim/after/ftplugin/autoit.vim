" AutoIt

" Commenting with ;
setlocal commentstring=;\ %s

" We have no syntax folding, so we'll revert to indent
" Note that it only indents *inside* the method, not the whole method
setlocal foldmethod=indent
