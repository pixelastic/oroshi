return {
  'akinsho/bufferline.nvim', 
  version = "4.9.1", 
  dependencies = {
    'nvim-tree/nvim-web-devicons'
  },
  config = function()
    local bufferline = require("bufferline")
    -- bufferline.setup({})

  end
}

