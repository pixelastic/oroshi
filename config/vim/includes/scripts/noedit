# This file contains the list of (extended) grep pattern to always exclude from
# the list returned by CtrlP. Typical files to exclude are images, binary and
# temporary files.
# All comments (starting with a #) are ignored.

# Images
\.(bmp|gif|ico|jpg|jpeg|pdf|png|svg)$

# Fonts
\.(otf|ttf|woff|eot)$

# Minified files
\.min\.(css|js|map)$

# Binaries
\.(fla|dll|exe|mo|pot|pyc|so|swf)$

# Dependencies
(node_modules|bower_components)

# Ebooks
\.(epub|mobi)$

# Versioning systems
\.(git|hg|svn)/
