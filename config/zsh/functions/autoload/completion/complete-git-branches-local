# List all local branches
function complete-git-branches-local() {
  local suggestions="$(\
    git branch \
      --sort=-committerdate \
      --format="%(refname:short):%(contents:subject)" \
  )"
  echo ${suggestions}
}
