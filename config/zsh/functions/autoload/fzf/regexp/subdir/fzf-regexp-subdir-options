# Returns the specific options to pass to fzf
function fzf-regexp-subdir-options() {
	fzf-regexp-options \
		--source fzf-regexp-subdir-source

	local prompt="$(fzf-regexp-prompt $PWD)"

	echo "--prompt=${prompt}"
	echo "--color=prompt:${COLOR_ALIAS_DIRECTORY}:regular"
}
