# Returns a list of matches for a query, in the current folder
function fzf-regexp-subdir-source() {
	local query="$1"
	local here="$PWD/"

	fzf-regexp-source \
		--directory "${here}" \
		--query "${query}"

}
