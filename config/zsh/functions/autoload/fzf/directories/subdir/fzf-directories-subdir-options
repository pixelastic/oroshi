# Returns the specific options to pass to fzf
function fzf-directories-subdir-options() {
	fzf-directories-options

	local fzfPrompt="$(fzf-prompt-directory $PWD)"

	echo "--prompt=${fzfPrompt}"
	echo "--color=prompt:${COLOR_ALIAS_DIRECTORY}:regular"
}
