# Open an image for visualization
# Note: This looks like a simple wrapper, but is used to define a custom
# completion method to only suggest image files
# Usage:
# $ img-open ./file.png           # Display file

function img-open() {
	gui eog $@
}
