# Remove the file from the staging area
# Usage:
# $ git-file-unstage ./relative/path/to/file  # Unstage the file
# $ git-file-unstage /absolute/path/to/file   # Unstage the file
function git-file-unstage() {
	git reset HEAD $@
}
