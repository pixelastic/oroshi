#compdef

# Triggered when trying to add a new link
function _yarn-link-universal() {
	# We suggest a filepath to a given directory, both for Classic (v1) and Berry
	# (v2+), as this is what the wrapper script expect

	_directories -W $PWD
}
