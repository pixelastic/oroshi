#compdef

# Suggest the available Docker images
function _docker-images-remote() {
  local suggestions=(${(f)"$(complete-docker-images-remote)"})

  _describe -V "$(completion-header $COLOR_ALIAS_DOCKER_IMAGE_REMOTE $COLOR_WHITE '   Remote Images ')" suggestions
}
