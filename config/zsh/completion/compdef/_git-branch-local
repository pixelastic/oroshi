#compdef

# Suggest the local branches
function _git-branch-local() {
  local suggestions=(${(f)"$(complete-git-branch-local)"})

  _describe -V "$(completion-header $COLOR_ALIAS_GIT_BRANCH_DEFAULT $COLOR_WHITE '  Local branches ')" suggestions
}
