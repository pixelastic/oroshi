#compdef

# Suggest the staged files
function _git-files-staged() {
  colors-load-definitions
  icons-load-definitions

  local suggestions=(${(f)"$(complete-git-files-staged)"})
  local label=" $ICONS[git-commit] Staged files "
  local header="$(completion-header $COLORS[git-modified] $COLORS[white] "$label")"
  _describe -V "$header" suggestions
}
