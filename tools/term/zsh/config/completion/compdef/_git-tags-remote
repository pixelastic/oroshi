#compdef

# Suggest the remote tags
function _git-tags-remote() {
  colors-load-definitions
  icons-load-definitions

  local suggestions=(${(f)"$(complete-git-tags-remote)"})
  local label=" $ICONS[git-tag] Remote tags "
  local header="$(completion-header $COLORS[git-remote] $COLORS[black] "$label")"
  _describe -V "$header" suggestions
}
