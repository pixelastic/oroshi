# List all local tags
setopt local_options err_return
local rawList="$(git-tag-list-raw)"

for rawLine in ${(f)rawList}; do
  local split=(${(@s/▮/)rawLine})
  local tagName=$split[1]
  local commitHash=$split[3]
  echo "${tagName}:${commitHash}"
done
