# List all remote Docker images
setopt local_options err_return

local outputFile=$OROSHI_ROOT/tools/docker/docker/config/data/src/images-remote.txt
local suggestions="$(cat $outputFile)"

echo "${suggestions//▮/:}"
