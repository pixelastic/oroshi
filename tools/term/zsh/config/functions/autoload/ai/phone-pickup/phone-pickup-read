# Fetch blocks content of a Notion page by ID
# Usage:
# $ phone-pickup-read <page_id>
setopt local_options err_return

local pageId="$1"

# Guard: page ID must be provided
[[ "$pageId" == "" ]] && return 1

notion-api "blocks/${pageId}/children"
