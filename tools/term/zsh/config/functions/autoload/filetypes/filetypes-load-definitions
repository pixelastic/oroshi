# Load FILETYPES associative array into memory
# No-op if already populated (allows test injection via mock)
# Usage: filetypes-load-definitions
setopt local_options err_return

# Stop if already loaded
((${#FILETYPES} > 0)) && return

source "$OROSHI_ROOT/tools/term/zsh/config/theming/dist/filetypes.zsh"
