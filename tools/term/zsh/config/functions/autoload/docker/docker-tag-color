# Returns the color of a given tag
# Usage:
# $ docker-tag-color latest
setopt local_options err_return

colors-load-definitions
local tagName="$1"

if [[ "$tagName" == "latest" ]]; then
	echo $COLORS[docker-tag-latest]
	return
fi

echo $COLORS[docker-tag]
