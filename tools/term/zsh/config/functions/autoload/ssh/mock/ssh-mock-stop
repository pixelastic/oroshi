# Stop and remove the mock SSH Docker container
# Usage:
# $ ssh-mock-stop
setopt local_options err_return

local containerName="oroshi-mock-ssh"

# Not running
if ! docker-container-is-running "$containerName"; then
	return 0
fi

docker stop "$containerName" >/dev/null
docker rm "$containerName" >/dev/null
