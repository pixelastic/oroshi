# Fetch a Google Doc and convert it to Markdown with images
# Usage:
# $ gdoc2md <url-or-doc-id>
setopt local_options err_return

local libDir="${functions_source[$0]:A:h}/__lib"
node "${libDir}/gdoc2md.js" "$@"
