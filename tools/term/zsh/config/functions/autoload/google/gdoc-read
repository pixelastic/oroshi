# Fetch a Google Doc and output Markdown to stdout
# Usage:
# $ gdoc-read <url-or-doc-id>
setopt local_options err_return

local libDir="${functions_source[$0]:A:h}/__lib"
node "${libDir}/gdocRead.js" "$@"
