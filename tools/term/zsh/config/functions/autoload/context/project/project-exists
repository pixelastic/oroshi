# Check if a project exists by name
# Usage:
# $ project-exists "aberlaas" && echo "exists"   # exits 0 if project is registered
setopt local_options err_return
local projectName="$1"

projects-load-definitions

# A project is real only if it has an icon
[[ "${PROJECTS[${projectName}:icon]}" != "" ]]
