# Convert a .jsonl file to .json
# Usage:
# $ jsonl2json masks.jsonl         # Creates masks.json
setopt local_options err_return

local inputFile="${1:a}"
local outputFile="${inputFile:r}.json"

jq \
  --slurp \
  '.' \
  "$inputFile" \
  > "$outputFile"
