# Play an audio file
# Usage:
# $ audio-play ./path/to/file.mp3  # Plays in background, no window
setopt local_options err_return

local input="$1"

process-detach ffplay \
  -nodisp \
  -autoexit \
  -loglevel quiet \
  $input
