# Check if "Sound Mode" is enabled
# Usage:
# $ sound-mode-is-enabled && echo "on"  # Exits 0 if enabled, 1 if disabled
setopt local_options err_return

local storeFile=$OROSHI_TMP_FOLDER/sound-mode/on

[[ -f $storeFile ]]
