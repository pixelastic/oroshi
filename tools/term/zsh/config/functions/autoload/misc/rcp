# Copy files using rsync
# Usage:
# $ rcp source destination
setopt local_options err_return

rsync \
  --verbose \
  --recursive \
  --archive \
  --human-readable \
  --partial \
  --progress \
  --modify-window=1 \
  "$@"
