# Check if a yarn install is currently running
# Usage:
# $ yarn-install-in-progress && echo "installing..."
setopt local_options err_return

git-dependencies-in-progress-lockfile node --reply
# Lockfile exists, install is running
[[ -r $REPLY ]] && return 0
return 1
