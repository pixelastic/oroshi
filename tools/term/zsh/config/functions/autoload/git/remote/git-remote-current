# Display name of current remote
# Usage:
# $ git-remote-current           # From cwd
# $ git-remote-current /path     # From given path
setopt local_options err_return

local targetPath="${1:-}"
[[ $targetPath == "" ]] && targetPath="$(git-directory-root)"

git-branch-remote --repo "$targetPath"
