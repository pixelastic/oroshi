# Pull all tags
# Usage:
# $ git-tag-pull                  # Pulls all remote tags
setopt local_options err_return
local currentRemote="$(git-remote-current)"
git fetch "${currentRemote}" 'refs/tags/*:refs/tags/*'
