# Options for the bats test fzf picker
setopt local_options err_return
local promptPrefix="$(colorize " 󰭟  Bats Tests " $COLOR_BLACK $COLOR_ALIAS_LANGUAGE_BATS)"
local promptSuffix="$(colorize "" $COLOR_ALIAS_LANGUAGE_BATS)"
echo "--prompt=${promptPrefix}${promptSuffix} "

echo "--delimiter=▮"
echo "--with-nth=2"
echo "--preview=fzf-fs-shared-preview {1}"
