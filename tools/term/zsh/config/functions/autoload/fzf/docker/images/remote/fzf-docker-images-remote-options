setopt local_options err_return

icons-load-definitions
colors-load-definitions

echo "--delimiter=   "
echo "--with-nth=1"
echo "--preview=fzf-docker-images-remote-preview {}"

# Prompt
local promptPrefix="$(colorize " $ICONS[docker-image-remote] Docker remote images " $COLORS[white] $COLORS[docker-image-remote])"
local promptSuffix="$(colorize "$ICONS[fzf-separator]" $COLORS[terminal] $COLORS[docker-image-remote])"
echo "--prompt=${promptPrefix}${promptSuffix} "
