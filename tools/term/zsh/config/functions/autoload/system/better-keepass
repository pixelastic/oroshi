# Open KeePass with GTK scaling
# Usage:
# $ better-keepass /path/to/keys.kdbx
setopt local_options err_return

# keepassxc uses GTK, and by default it is not honoring the Ubuntu scale factor,
# so we force it here
QT_SCALE_FACTOR=1.3 keepassxc "$@"
