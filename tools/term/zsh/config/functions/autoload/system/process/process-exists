# Check if a process with the given PID is alive
# Usage:
# $ process-exists        # Current process
# $ process-exists 1234
setopt local_options err_return

# Default to current process
local pid="${1:-$$}"

# Check /proc entry
[[ -d "/proc/$pid" ]]
