# Get the current workspace number
# Usage:
# $ workspace-current   # 5
setopt local_options err_return

# Note: wmctrl uses 0-indexed workspaces
local currentIndex=$(wmctrl -d | grep '\*' | awk '{print $1}')
echo $((currentIndex + 1))
