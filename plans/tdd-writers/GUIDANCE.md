## Guidance

- All files are skill definitions at `tools/ai/claude/config/skills/<name>/SKILL.md`
- These are markdown config files — no code, no tests
- Use `/skill-writer` when editing SKILL.md files
- Reference js-writer as the canonical TDD structure template
- Edit files in the worktree path, never via `~/.claude/skills/` symlinks

## Discoveries
