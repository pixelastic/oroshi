## Guidance

- Lint check: `yarn run lint`
- No tests to write or run
- Delete symlinks with `rm`, not `git rm` (git tracks the symlink target)
- When editing `kitty-restore`, remove the full commented-out block including surrounding blank lines
- When editing `keybindings.conf`, remove only the Alt+F5 line

## Discoveries
